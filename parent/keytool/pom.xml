<?xml version="1.0" encoding="UTF-8"?>

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.maven.resource.parent</groupId>
        <artifactId>parent</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>keytool</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>Java Keytool</name>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>keytool-maven-plugin</artifactId>
                    <version>1.5</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <profiles>
        <profile>
            <id>keytool</id>
            <build>
                <pluginManagement>
                    <plugins>
                        <plugin>
                            <groupId>org.codehaus.mojo</groupId>
                            <artifactId>keytool-maven-plugin</artifactId>
                            <executions>
                                <execution>
                                    <id>clean</id>
                                    <phase>generate-resources</phase>
                                    <goals>
                                        <goal>clean</goal>
                                    </goals>
                                    <inherited>true</inherited>
                                </execution>
                                <execution>
                                    <id>generateKeyPair</id>
                                    <phase>generate-resources</phase>
                                    <goals>
                                        <goal>generateKeyPair</goal>
                                    </goals>
                                    <inherited>true</inherited>
                                </execution>
                            </executions>
                            <inherited>true</inherited>
                            <configuration>
                                <keystore>${project.build.directory}/keystore</keystore>
                                <dname>cn=localhost, ou=None, L=Scottsdale, ST=Arizona, o=EXAMPLE, c=US</dname>
                                <keypass>EXAMPLE</keypass>
                                <storepass>EXAMPLE</storepass>
                                <alias>EXAMPLE</alias>
                                <sigalg>SHA1withRSA</sigalg>
                            </configuration>
                        </plugin>
                    </plugins>
                </pluginManagement>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>keytool-maven-plugin</artifactId>
                        <inherited>true</inherited>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
